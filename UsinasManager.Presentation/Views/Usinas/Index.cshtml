@model IEnumerable<UsinasManager.Presentation.ViewModels.UsinaViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ativo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FornecedorId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ativo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FornecedorId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UsinaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UsinaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UsinaId })
        </td>
    </tr>
}

</table>
